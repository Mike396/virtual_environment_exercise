from setuptools import setup, find_packages

setup(
    name='GithubPackages',
    version='1.0.0',
    url='https://github.com/Mike396/virtual_environment_exercise',
    author='mks',
    packages=find_packages(), # ['GithubPackages', ]
)
